require('dotenv').config({ path: '.env' }); const mongoose = require('mongoose'); const { getDishCosting } = require('./src/controllers/costing.controller.ts'); mongoose.connect(process.env.MONGO_URI).then(async () => { const db = mongoose.connection.db; const userId = new mongoose.Types.ObjectId('69f84eb97a1102e857341078'); const dish = await db.collection('dishes').findOne({ userId, category: 'Chinese' }); const req = { user: { userId: userId.toString() }, params: { dishId: dish._id.toString() } } as any; const res = { json: (data: any) => console.log('Result:', JSON.stringify(data, null, 2)), status: (code: any) => ({ json: (err: any) => console.log('Error', code, err) }) } as any; await getDishCosting(req, res); mongoose.disconnect(); });
